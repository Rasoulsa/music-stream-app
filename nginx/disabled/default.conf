# Intentionally empty.
#
# On the VPS HTTPS overlay (docker-compose.vps.yml) this file overrides the
# plain HTTP config that docker-compose.prod.yml mounts at
# /etc/nginx/conf.d/default.conf.
#
# The real HTTPS config is generated from nginx/app-ssl.conf.template into
# /etc/nginx/conf.d/app-ssl.conf via the official nginx image's envsubst step.
