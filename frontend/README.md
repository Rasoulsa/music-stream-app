# Music Stream App — Frontend

React + TypeScript frontend for the Music Stream App, built with Vite.

## Tech Stack

- React 18 + TypeScript
- Vite
- Axios (API client)
- ESLint + Prettier

## Setup

```bash
npm install
cp .env.example .env
